
<section class="channels-list">
  <h1> Results for '<%= @query %>'</h1>
  <% if @recipes %>
    <ul>
      <% @recipes.each do |recipe| %>
        <li>
          <%= link_to recipe.label, recipe_show_path(recipe) %>
          <%= link_to image_tag(recipe.image, :size => "300x300", alt:"recipe image"), recipe_show_path(recipe) %>
        </li>

      <% end %>
    </ul>
  <% end %>
</section>
