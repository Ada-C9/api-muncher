<h2>Api-tizers!</h2>

<ul>
  <% @recipes.each do |recipe| %>
    <li class="thumbnail">
      <div>
        <%= image_tag recipe.image %>
      </div>
      <p><%= link_to recipe.label, recipe_path(recipe.uri) %></p>
    </li>
  <% end %>
</ul>

<%= paginate @recipes %>
