<section class="recipes-list">
  <h1>Things to Cook</h1>
  <% if @recipes %>
    <ul>
      <% @recipes.each do |recipe| %>
        <li>
          <%= recipe.uri %>
          <%= image_tag recipe.image %>
          <%= link_to recipe.label, recipe_path(:uri => recipe.uri, :label => recipe.label) %>
        </li>
      <% end %>
    </ul>
  <% end %>
</section>
