<section class="recipes-list">
  <h1>Recipes</h1>
  <% if @recipes %>
    <ul>
      <% @recipes.each do |recipe| %>
        <li>
          <%= link_to recipe.label, recipe_new_path(recipe.id)%>
        </li>
      <% end %>
    </ul>
  <% end %>
</section>
