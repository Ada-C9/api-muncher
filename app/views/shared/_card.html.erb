<div class='card small-10 small-offset-1 medium-6 large-3'>
  <div class="card-divider">
    <p><%= item.name %></p>
  </div>
  <%= link_to image_tag(item.image), recipe_path(uri: item.uri), class: 'card-picture' %>
</div>
