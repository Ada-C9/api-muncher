<h2>Api-tizers!</h2>

<ul>
  <%= @recipes.each do |recipe| %>
    <li class="thumbnail">
      <div>
        <img src=recipe.url alt=recipe.label>
      </div>
      <p> insert link to recipe </p>
    </li>
  <% end %>
</ul>
