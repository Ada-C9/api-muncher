<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "CookingThyme" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>

    <script src="https://developer.edamam.com/attribution/badge.js"></script>
  </head>

  <body>

    <header>
      <h2 id="site-title">
        <% link_to "Cooking Thyme", root_path %>
        <strong id="slogan">There is always thyme for cooking</strong>
      </h2>
      <% if request.fullpath != root_path %>
      <nav>
        <%= render partial: "form" %>
      </nav>
      <% end %>
    </header>

    <main>

    <% if flash[:status] %>
       <section class="row status <%= flash[:status] %>">
         <h3>
           <%= flash[:message] %>
         </h3>
        </section>
    <% end %>


    <%= yield %>
    </main>

  </body>

  <footer id="footer">
    <section>
      <div id="edamam-badge" data-color="white"></div>
    </section>
  </footer>
</html>
