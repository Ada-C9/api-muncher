<h2>Recipes for <%= @query %></h2>

<section>
  <% if @recipes %>
  <ul>
    <% @recipes.each do |recipe| %>
    <li>
      <%= link_to recipe.label, recipe_path(uri: recipe.uri) %>
    </li>
    <% end %>
  </ul>
  <% else %>
  <p>
  None found!
  </p>
  <% end %>
</section>
