<div class="root">
  <h1>Website Title</h1>
  <%= form_tag(search_results_path, method: :get) do %>
      <div class="search"><%= text_field_tag :search, params[:search], placeholder: "Search Recipes" %></div>
      <div class="submit"><%= submit_tag "Search", :name => nil, class: "button" %></div>
  <% end %>
</div>
