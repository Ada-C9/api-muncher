<h2>Results for "<%= @query.titlecase %>"</h2>
<section class="results">
  <% if @results %>
    <% @results.each do |result| %>
    <article class="results item">
      <%= image_url(result) %>
      <% @recipe = result %>
      <%= link_to result.label, show_path(result.uri.split('owl')[1]), class: "recipe-label" %>
    </article>
    <% end %>
  <% end %>
</section>
<%= paginate @results, window: 2 %>
