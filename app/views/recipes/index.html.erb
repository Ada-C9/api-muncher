<h1>Results for " "</h1>

<section class="index">
  <% @recipes.each do |recipe| %>
  <div>
    <%= image_tag(recipe.image) %>
  </div>
  <div>
    <%= recipe.title %>
  </div>
  <% end %>
</section>
